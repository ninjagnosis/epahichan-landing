import { useState } from 'react';
const links = [['Platform','#platform'],['For Individuals','#individuals'],['For Business','#business'],['Developers','#developers'],['Trust & Security','#trust'],['Help','#help']];
export default function MenuButton() { const [open,setOpen]=useState(false); return <div className="menu-wrap"><button className="menu-button" type="button" aria-expanded={open} aria-controls="mobile-nav" onClick={()=>setOpen(!open)}><span className="sr-only">Toggle navigation</span><span /><span /></button>{open && <nav id="mobile-nav" className="mobile-nav" aria-label="Mobile navigation">{links.map(([label,href])=><a href={href} onClick={()=>setOpen(false)} key={href}>{label}</a>)}<a className="mobile-contact" href="#contact" onClick={()=>setOpen(false)}>Get in touch</a></nav>}</div>; }
